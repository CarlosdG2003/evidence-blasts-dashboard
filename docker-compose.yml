version: '3.8'

services:
  evidence:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./sources/parquets:/app/sources/parquets
      - ./pages:/app/pages
    environment:
      - NODE_ENV=development
    restart: unless-stopped